{
  "name": "jal-flight-board",
  "version": "1.0.0",
  "private": true,
  "description": "JAL flight status dashboard powered by ODPT",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js && node tests/frontend.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
